server:
  servlet:
    context-path: /archetypemgm
spring:
  application:
    name: archetype-mgm
  profiles:
    active: ${env}
    include: dao-${env},redis-${env},ref-${env},mgm-${env}
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
logging:
  config: classpath:logback-spring.xml
  file:
    name: /opt/logs/${spring.application.name}/${spring.application.name}.log
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
info:
  app:
    name: '@project.name@'
    version: '@project.version@'
    java:
      version: '@java.version@'
